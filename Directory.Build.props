<Project>
  <PropertyGroup>
    <!-- Centralized clean locations -->
    <OutDir>$(MSBuildThisFileDirectory)artifacts\bin\$(Configuration)\</OutDir>
    <OutputPath>$(MSBuildThisFileDirectory)artifacts\bin\$(Configuration)\</OutputPath>
    <IntDir>$(MSBuildThisFileDirectory)artifacts\obj\$(MSBuildProjectName)\$(Configuration)\</IntDir>

    <!-- Disables the automatic framework/runtime folder suffixes for WPF -->
    <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    <AppendRuntimeIdentifierToOutputPath>false</AppendRuntimeIdentifierToOutputPath>
  </PropertyGroup>
</Project>
